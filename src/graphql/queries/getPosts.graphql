# import "../../components/ui/Posts/index.tsx"

query GetPosts($page: Int, $perPage: Int) {
  published {
    posts(page: $page, perPage: $perPage) {
      posts {
        ...postsTopFragment
      }
      pagination {
        totalCount
        limitValue
        totalPages
        currentPage
      }
    }
  }
}
